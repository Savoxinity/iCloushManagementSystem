.page-container { padding: 24rpx; background: #0a0a0a; min-height: 100vh; }
.header-bar { display: flex; justify-content: space-between; align-items: center; margin-bottom: 24rpx; }
.page-title { font-size: 36rpx; color: #fff; font-weight: bold; }
.btn-create { font-size: 26rpx; color: #000; background: #C9A84C; padding: 12rpx 28rpx; border-radius: 8rpx; font-weight: bold; }
.filter-bar { display: flex; gap: 16rpx; margin-bottom: 32rpx; }
.filter-item { font-size: 24rpx; color: #888; padding: 10rpx 24rpx; border-radius: 24rpx; background: rgba(255,255,255,0.05); }
.filter-item.active { background: #C9A84C; color: #000; font-weight: bold; }
.empty-state { display: flex; flex-direction: column; align-items: center; padding: 120rpx 0; }
.empty-icon { font-size: 80rpx; margin-bottom: 24rpx; }
.empty-text { font-size: 28rpx; color: #666; margin-bottom: 32rpx; }
.btn-action { font-size: 28rpx; color: #C9A84C; border: 1rpx solid #C9A84C; padding: 16rpx 48rpx; border-radius: 8rpx; }
.expense-card { background: rgba(255,255,255,0.05); border-radius: 12rpx; padding: 24rpx; margin-bottom: 20rpx; }
.card-top { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16rpx; }
.exp-purpose { font-size: 30rpx; color: #fff; font-weight: bold; }
.exp-status { font-size: 24rpx; padding: 4rpx 16rpx; border-radius: 6rpx; }
.status-pending { background: rgba(251,191,36,0.15); color: #FBBF24; }
.status-approved { background: rgba(16,185,129,0.15); color: #10B981; }
.status-rejected { background: rgba(239,68,68,0.15); color: #EF4444; }
.card-body { display: flex; align-items: baseline; gap: 16rpx; margin-bottom: 12rpx; }
.exp-amount { font-size: 40rpx; color: #C9A84C; font-weight: bold; }
.exp-type { font-size: 24rpx; color: #666; }
.card-bottom { display: flex; justify-content: space-between; }
.exp-date { font-size: 22rpx; color: #555; }
.exp-points { font-size: 24rpx; font-weight: bold; }
.exp-points.positive { color: #10B981; }
.exp-points.negative { color: #EF4444; }
