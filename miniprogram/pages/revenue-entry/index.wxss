/* 营收直录 — 品牌黑橙配色 + 编辑/删除 */
.page-container {
  min-height: 100vh;
  background: #111111;
  padding: 32rpx;
  box-sizing: border-box;
}

.page-title {
  font-size: 40rpx;
  font-weight: 700;
  color: #E8A945;
  margin-bottom: 32rpx;
}

/* 编辑模式提示条 */
.edit-banner {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: rgba(232, 169, 69, 0.15);
  border: 1rpx solid rgba(232, 169, 69, 0.3);
  border-radius: 12rpx;
  padding: 16rpx 24rpx;
  margin-bottom: 24rpx;
}
.edit-banner-text { font-size: 24rpx; color: #E8A945; }
.edit-banner-cancel {
  font-size: 24rpx;
  color: #EF4444;
  padding: 4rpx 16rpx;
  border: 1rpx solid rgba(239, 68, 68, 0.3);
  border-radius: 6rpx;
}

.form-section {
  background: #1E1E1E;
  border-radius: 16rpx;
  padding: 8rpx 24rpx;
  border: 1rpx solid rgba(232, 169, 69, 0.15);
}

.form-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 28rpx 0;
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.06);
}

.form-item:last-child {
  border-bottom: none;
}

.label {
  font-size: 28rpx;
  color: #CCCCCC;
  flex-shrink: 0;
}

.required {
  color: #E8A945;
  margin-left: 4rpx;
}

.input {
  text-align: right;
  font-size: 28rpx;
  color: #FFFFFF;
  flex: 1;
  margin-left: 20rpx;
}

.picker-value {
  font-size: 28rpx;
  color: #E8A945;
}

.btn-area {
  margin-top: 40rpx;
  display: flex;
  justify-content: center;
}

.btn-submit {
  width: 60%;
  height: 80rpx;
  line-height: 80rpx;
  text-align: center;
  background: linear-gradient(135deg, #E8A945, #D4942E);
  color: #111111;
  font-size: 30rpx;
  font-weight: 600;
  border-radius: 40rpx;
  border: none;
}

.btn-disabled {
  opacity: 0.4;
}

.recent-section {
  margin-top: 48rpx;
}

.section-title {
  font-size: 26rpx;
  color: #999999;
  margin-bottom: 16rpx;
}

.recent-item {
  display: flex;
  align-items: center;
  background: #1E1E1E;
  border-radius: 12rpx;
  margin-bottom: 12rpx;
  border: 1rpx solid rgba(232, 169, 69, 0.08);
  overflow: hidden;
}

.recent-main {
  flex: 1;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx;
  min-width: 0;
}

.recent-left {
  display: flex;
  flex-direction: column;
  flex: 1;
  min-width: 0;
}

.recent-cat {
  font-size: 28rpx;
  color: #FFFFFF;
  font-weight: 600;
}

.recent-desc {
  font-size: 22rpx;
  color: #888888;
  margin-top: 4rpx;
}

.recent-amount {
  font-size: 32rpx;
  font-weight: 700;
  color: #4CAF50;
  font-family: 'DIN Alternate', monospace;
  flex-shrink: 0;
  margin-left: 16rpx;
}

/* 操作按钮 */
.recent-actions {
  display: flex;
  flex-shrink: 0;
}

.action-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100rpx;
  height: 100%;
  min-height: 100rpx;
  font-size: 22rpx;
  font-weight: 600;
}

.action-edit {
  background: rgba(232, 169, 69, 0.15);
  color: #E8A945;
}

.action-delete {
  background: rgba(239, 68, 68, 0.15);
  color: #EF4444;
}

.empty-hint {
  text-align: center;
  color: #666666;
  font-size: 26rpx;
  margin-top: 80rpx;
}
