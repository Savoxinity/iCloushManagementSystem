# ═══════════════════════════════════════════════════
# iCloush 智慧工厂后端 — Python 依赖
# Phase 5.1: 云端迁移适配
# ═══════════════════════════════════════════════════

# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# 数据库
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2

# 配置
pydantic-settings==2.5.2
python-dotenv==1.0.1

# 认证
PyJWT==2.9.0

# HTTP 客户端
httpx==0.27.2

# Redis（可选，未配置时自动降级为内存存储）
redis==5.1.1

# 腾讯云 COS 对象存储（Phase 5.1 新增）
cos-python-sdk-v5>=1.9.30

# 腾讯云 OCR 发票识别（Phase 3 Hotfix）
tencentcloud-sdk-python>=3.0.1000

# 工具
orjson==3.10.7
